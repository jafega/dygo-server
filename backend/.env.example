# Dev backend env example
# PORT=3001

# Optional SMTP configuration (nodemailer)
# SMTP_HOST=smtp.mailprovider.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-smtp-username
# SMTP_PASS=your-smtp-password
# SMTP_FROM="Dygo <no-reply@yourdomain.com>"
#
# Google OAuth (optional)
# GOOGLE_CLIENT_ID=your-google-oauth-client-id
#
# Quick dev tip: run MailHog (open-source dev SMTP + web UI)
# - With Docker: `docker run --rm -p 1025:1025 -p 8025:8025 mailhog/mailhog`
# - Then set:
#   SMTP_HOST=localhost
#   SMTP_PORT=1025
#   SMTP_SECURE=false
#   SMTP_FROM="Dygo <no-reply@local>"
# - View caught emails at: http://localhost:8025
#
# Automatic Ethereal fallback (no configuration needed):
# - If no SMTP variables are set and NODE_ENV != 'production', the server will create an Ethereal test account automatically and use it to send emails. A preview URL will be logged (and returned in API responses) so you can view the message without any setup.
# - Ethereal is for testing only (https://ethereal.email/)

# Other envs
# NODE_ENV=development
