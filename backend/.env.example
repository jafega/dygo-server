# Dev backend env example
# PORT=3001

# Google OAuth (optional)
#
# Demo password reset secret (optional):
# - In production set PASSWORD_RESET_SECRET to a strong secret; the reset endpoint will require it.
# - In development (NODE_ENV!=production) the endpoint is allowed without a secret for convenience.
# PASSWORD_RESET_SECRET=some-secret
# Stripe keys (test)
# STRIPE_SECRET_KEY=sk_test_xxx
# STRIPE_PUBLISHABLE_KEY=pk_test_xxx
# STRIPE_WEBHOOK_SECRET=whsec_xxx  # optional but recommended for webhook verification
# Optionally set an existing Price ID for the EUR €9.99 monthly plan to reuse it instead of creating one on the fly
# STRIPE_PRICE_ID_EUR=price_xxx

# Frontend URL for success/cancel/portal redirects
# FRONTEND_URL=http://localhost:5173
# For local webhook testing you can use the Stripe CLI or post fixtures directly:
# - Stripe CLI: `stripe listen --forward-to http://localhost:3001/webhook` (copy signing secret into STRIPE_WEBHOOK_SECRET if you want signature verification)
# - Post fixture helper: `node scripts/post_test_event.js test/fixtures/invoice_payment_failed.json`

# Other envs
# NODE_ENV=development
